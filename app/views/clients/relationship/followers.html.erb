  <div class="container px-5 px-sm-0">
    <div class="row">
      <div class="col-md-8">
        <h2>Follower Users</h2>
        <% if @client.exists? %>
          <table>
            <thead>
              <tr>
                <th>name</th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% @client.each do |client| %>
                <tr>
                  <td><%= client.name %></td>
                  <td>フォロー数: <%= client.followings.count %></td>
                  <td>フォロワー数: <%= client.followers.count %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        <% else %>
          <p>ユーザーはいません</p>
        <% end %>
      </div>
    </div>
  </div>